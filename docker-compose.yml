version: '3.8'

services:
  secure-boot-app:
    build: .
    ports:
      - "8080:8080"
    user: "1000:1000"
    cap_add:
      - NET_BIND_SERVICE
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,size=100m
    environment:
      - PORT=8080
    restart: unless-stopped
